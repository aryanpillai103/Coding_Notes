
# Brute Force

map<int , int> mpp;
for(i = 0 -> n-1)
	mpp[nums[i]++]
ans = []

for(it : mpp)
	if(it.second == 1)
		ans.add(it.first);
return ans;


TC: O(nlogm+m) , m= (n/2)+1
SC: O(m)


# Optimised 

